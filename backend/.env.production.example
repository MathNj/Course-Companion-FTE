# Production Environment Variables Template
# DO NOT commit actual values to git
# Copy this to .env.production and fill in real values

# App Configuration
APP_NAME="Course Companion FTE"
APP_ENV=production
DEBUG=false

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_V1_PREFIX=/api/v1

# Security
JWT_SECRET_KEY=your-super-secret-key-generate-with-secrets.token_urlsafe-32
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=43200
REFRESH_TOKEN_EXPIRE_MINUTES=43200

# Database (from platform)
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/database

# Redis (from Upstash or platform)
REDIS_URL=redis://user:password@host:6379

# CORS
CORS_ORIGINS=https://chat.openai.com,https://chatgpt.com
CORS_ALLOW_CREDENTIALS=true

# Logging
LOG_LEVEL=INFO

# Content
CONTENT_SOURCE=local
CONTENT_CACHE_TTL=86400

# Cloudflare R2 (Optional - for production content storage)
R2_ENDPOINT_URL=https://your-account-id.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=your-r2-access-key
R2_SECRET_ACCESS_KEY=your-r2-secret-key
R2_BUCKET_NAME=course-companion-content

# Stripe (Future - for subscriptions)
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_PUBLISHABLE_KEY=pk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Generate JWT_SECRET_KEY with:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

# Platform-specific notes:
# - Fly.io: Use fly secrets set KEY=value
# - Railway: Use railway variables set KEY=value
# - Render: Use dashboard Environment section
